<%= form_for(current_wish.list.build, remote: true) do |f| %>
  <div><%= hidden_field_tag :product_id, @product.id %></div>
  <div><%= hidden_field_tag :variant_id, @variant.id %></div>
  <li class="list-inline-item">
    <button type="submit" name="commit" value="Add to Wishlist" class="btn btn-template-outlined" data-disable-with="Add to Wishlist">
      <i class="fa fa-heart-o"></i>Add to Wishlist
    </button>
  </li>
<% end %>
